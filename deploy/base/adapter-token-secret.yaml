apiVersion: v1
kind: Secret
metadata:
  name: adapter-internal-token
  namespace: opencode-system
type: Opaque
stringData:
  # Intentionally empty: replace this Secret with a secretGenerator or an
  # encrypted secret controller before deployment. Both services reject it.
  token: ""
