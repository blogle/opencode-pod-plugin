apiVersion: cert-manager.io/v1
kind: Certificate
metadata:
  name: opencode-tls
  namespace: opencode-system
spec:
  secretName: opencode-tls
  dnsNames:
    - opencode.thejeffer.net
    - "*.opencode.thejeffer.net"
  issuerRef:
    kind: ClusterIssuer
    name: letsencrypt
