# Add this to a private production overlay. Do not commit adapter-token.env.
secretGenerator:
  - name: adapter-internal-token
    namespace: opencode-system
    behavior: replace
    envs:
      - adapter-token.env
