# Generate a random value, for example: openssl rand -hex 32. Keep the real
# input outside Git, or manage the resulting Secret with SealedSecrets/SOPS.
token=REPLACE_WITH_A_RANDOM_INTERNAL_ADAPTER_TOKEN
